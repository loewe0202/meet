<!-- 这是自定义组件的内部WXML结构 -->
<view class="inner">
    <slot name="before"></slot>
    <view class="my-class">这里是组件的内部细节，这段文字的颜色由组件外的class决定 -- {{propB.date}}</view>
    <slot name="after"></slot>
</view>